{
  "name": "string.bullet",
  "version": "1.0.12",
  "description": "Appends a bullet before string (to mimick lists in ASCII)",
  "main": "string.bullet.js",
  "scripts": {
    "lint": "eslint string.bullet.js",
    "lint-test": "eslint test.js",
    "test": "npm run lint && npm run lint-test && nyc --reporter=html --reporter=text mocha --reporter spec",
    "autotest": "mocha --reporter spec --watch",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/xpl/string.bullet.git"
  },
  "keywords": [
    "string",
    "bullet"
  ],
  "author": "Vitaly Gordon <rocket.mind@gmail.com>",
  "license": "Unlicense",
  "homepage": "https://github.com/xpl/string.bullet",
  "dependencies": {
    "printable-characters": "^1.0.26"
  },
  "devDependencies": {
    "mocha": "^3.4.2",
    "coveralls": "^2.13.1",
    "eslint": "^4.2.0",
    "istanbul": "^0.4.5",
    "nyc": "^11.1.0"
  }
}
